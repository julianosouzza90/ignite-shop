import { styled } from '@/styles'

export const HeaderContainer = styled('header', {
  width: '100%',
  maxWidth: 1180,

  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
})
